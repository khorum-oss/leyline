/**
 * Test doubles for the core contracts, published so that adapter authors
 * outside this repository can hold themselves to the same conformance bar.
 */
export { createFakeStore, type FakeStore } from './fake-store.js';
