---
'@leyline/schema': minor
'@leyline/core': patch
---

Schema foundation: the canonical workflow document, graph and capability validation, deterministic addressing, change descriptions, and published JSON Schema artifacts. `@leyline/core` now takes its issue shape from `@leyline/schema`, so a binding failure and a validation issue are one format.
