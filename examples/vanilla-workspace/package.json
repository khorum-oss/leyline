{
  "name": "@leyline-examples/vanilla-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "The brief's §2 scenario with no framework at all — the same document, rendered straight into the DOM.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@leyline-examples/scenario": "workspace:*",
    "@leyline/core": "workspace:*",
    "@leyline/vanilla": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.14"
  }
}
