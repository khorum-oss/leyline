{
  "name": "@leyline-examples/agent-cli",
  "version": "0.0.0",
  "private": true,
  "description": "Brief \u00a72 items 6 and 7, driven by an agent. Runs scripted or interactive with no API key; runs against Claude with one.",
  "type": "module",
  "bin": {
    "leyline-agent": "./dist/index.js"
  },
  "scripts": {
    "demo": "tsx src/index.ts demo",
    "repl": "tsx src/index.ts repl",
    "chat": "tsx src/index.ts chat",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@leyline-examples/scenario": "workspace:*",
    "@leyline/agent": "workspace:*",
    "@leyline/core": "workspace:*"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.125.0",
    "@types/node": "^22.20.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
